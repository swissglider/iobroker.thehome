const initConnectionChecker = async (adapter: ioBroker.Adapter): Promise<void> => {
	adapter;
	return;
};

const stopConnectionChecker = async (adapter: ioBroker.Adapter): Promise<void> => {
	adapter;
	return;
};

const ConnectionChecker = {
	stopConnectionChecker: stopConnectionChecker,
	initConnectionChecker: initConnectionChecker,
};

export default ConnectionChecker;
